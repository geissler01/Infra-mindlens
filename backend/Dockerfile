# Placeholder Dockerfile (Reemplazar con código .NET real)
FROM mcr.microsoft.com/dotnet/aspnet:8.0-alpine AS base
WORKDIR /app
EXPOSE 80

# Esto es temporal para que docker-compose up no falle.
# El equipo de C# debe borrar esto y usar su Dockerfile oficial.
FROM alpine
CMD ["sh", "-c", "echo 'Backend C# Placeholder. Reemplaza este Dockerfile con tu código.' && tail -f /dev/null"]
